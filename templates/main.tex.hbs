\documentclass{article}

\usepackage{xhfill}
\usepackage[hidelinks]{hyperref}
\usepackage{fontawesome}
\usepackage[T1]{fontenc}
\usepackage[margin=4em]{geometry}
\usepackage{parskip}

\usepackage[maxnames=100, giveninits=true]{biblatex}
\addbibresource{publications.bib}

\pagestyle{empty}

\usepackage[explicit]{titlesec}
\titleformat{\section}
	{\Large \scshape}
	{}
	{0em}  % number-header spacing 
	{#1 \ \xrfill[.2em]{.5pt}}
\titlespacing{\section}
	{-1em}  % leading
	{.8em}  % before
	{.4em}  % after

\usepackage{enumitem}
\setlist[itemize]{
	label={\bfseries-},
	leftmargin=1.5em,
	topsep=-.1em,
	itemsep=-.3em,
}

%  -- TITLE --

\makeatletter
\renewcommand{\maketitle}{
\setlength{\leftskip}{-1em}

{\huge \scshape \@author}

\vspace{-.5em}
\rule{.4 \textwidth}{0.5pt}

{
{\it \phone}
\ | \
{\it \email}
\ | \
\href{http://\website}{\it \website}
}

\setlength{\leftskip}{0em}
}
\makeatother


\author{ {{~about.name~}} }
\def\phone{ {{~about.phone~}} }
\def\email{ {{~about.email~}} }
\def\github{ {{~about.github~}} }
\def\website{ {{~about.website~}} }

\begin{document}

\maketitle

{{#each sections}}
	\section{ {{~this.title~}} }
	\label{ {{~this.key~}} }

	{{{this.summary}}}

	{{#each this.items}}
		\def\empty{}

		\def\title{%
			{{{this.title}}}%
		}

		\def\subtitle{%
			{{{this.subtitle}}}%
		}

		\def\notes{%
			{{{this.notes}}}%
		}

		\def\paren{%
			{{{this.paren}}}%
		}

		
		\def\dates{%
			{{{this.dates}}}%
		}

		{{#if this.github}}
			\def\paren{%
				\href{https://github.com/\github/{{this.github~}} }
				     {\hspace{2pt}\faicon{github} \github/{{this.github~}}\hspace{2pt}}%
			}
		{{/if}}

		\ifx\title\empty{}\else{\textbf{\title}}\fi%
		\ifx\subtitle\empty{}\else{, \textit{\subtitle}}\fi%
		\ifx\notes\empty{}\else{: \notes}\fi%
		\ifx\paren\empty{}\else{ (\textit{\paren})}\fi%
		\ifx\dates\empty{}\else{\hfill \textit{\dates}}\fi
 
		{{#if this.bullets}}
			\begin{itemize}
			{{#each this.bullets}}
				\item {{{this}}}
			{{/each}}
			\end{itemize}
		{{/if}}

		{{#if this.citation}}
			\fullcite{ {{~this.citation~}} }
		{{/if}}
	{{/each }}
{{/each }}

\end{document}
